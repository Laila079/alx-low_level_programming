#include <stdio.h>
#include <string.h>  

#include "main.h"


/**
 * _strlen - strlen of pointer (str)
 * @s: char pointer
 * Return: lentgh of str.
*/

int _strlen(char *s)
{
int len;

len = strlen(s);

return (len);
}
